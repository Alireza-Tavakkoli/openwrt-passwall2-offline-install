# آموزش نصب Passwall2 به صورت دستی روی OpenWrt

## مقدمه
در این آموزش یاد می‌گیرید چگونه Passwall2 را به صورت دستی (Manual) روی OpenWrt نصب کنید.

این روش برای زمانی است که:
- اینترنت روی روتر ندارید
- یا می‌خواهید فایل‌ها را خودتان نصب کنید
- یا opkg به درستی کار نمی‌کند

---

## ⚠️ قبل از شروع

باید معماری دستگاه خود را بدانید، چون اگر فایل اشتباه نصب شود، Passwall2 اجرا نمی‌شود.

### خطاهای رایج:
- incompatible with the architectures
- dependency error
- installation failed

---

## 📌 مرحله 1: پیدا کردن مشخصات دستگاه

وارد SSH شوید و دستور زیر را بزنید:

```bash
cat /proc/cpuinfo

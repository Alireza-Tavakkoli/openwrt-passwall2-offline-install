# آموزش نصب Passwall2 به صورت دستی روی OpenWrt


## مقدمه
در این آموزش یاد می‌گیرید چگونه Passwall2 را به صورت دستی (Manual) روی OpenWrt نصب کنید.

این روش برای زمانی است که:
- اینترنت آزاد بر روی روتر ندارید یا در کشوری زندگی میکنید که از همجا فیلتر است مثلا ایران
- یا می‌خواهید فایل‌ها را خودتان نصب کنید
- یا ابزار opkg / apk به درستی کار نمی‌کند


## ⚠️ حداقل مشخصات مورد نیاز

قبل از نصب، مطمئن شوید دستگاه شما توانایی اجرای Passwall2 را دارد:

### 🔹 رم (RAM)
- حداقل قابل اجرا: 128MB  
- پیشنهادی: 256MB یا بیشتر  
- کمتر از 128MB → احتمال لگ، کرش یا اجرا نشدن سرویس‌ها

### 🔹 پردازنده (CPU)
- حداقل پیشنهادی: 700MHz به بالا  
- بهتر: دو هسته‌ای یا 1GHz+

### 🔹 حافظه (Storage)
- حداقل: 16MB فضای خالی (لب مرز)  
- پیشنهادی: 32MB تا 128MB  
- چون Passwall2 حجم می‌گیرند

### 🔹 نسخه OpenWrt
- حداقل: OpenWrt 21.02  
- پیشنهادی: 23 / 24 / 25  


## 💻 نرم‌افزارهای مورد نیاز روی کامپیوتر

برای انجام مراحل نصب، باید از طریق SSH به روتر وصل شوید. برای این کار یکی از ابزارهای زیر را روی کامپیوتر نصب کنید:



### 🟢 Windows :

### 1. PuTTY

### 2. WinSCP 


## 🔌 مرحله 0: اتصال به روتر با PuTTY (SSH)

قبل از هر کاری باید به روتر OpenWrt وصل شوید.

---
### 📌 مراحل اتصال

1. برنامه PuTTY را باز کنید
2. در قسمت **Host Name** این را وارد کنید:

```
192.168.1.1
```

3. Port را روی این مقدار بگذارید:

```
22
```

4. Connection type را روی:

```
SSH
```

5. روی **Open** کلیک کنید

---

### 🔑 ورود به روتر

وقتی صفحه سیاه باز شد:

* Username:

```
root
```

* Password:

```
رمز روتر (اگر تنظیم نکردی خالی بگذار)
```

---

## 🟡 روش دوم: Windows Terminal (جایگزین PuTTY)

اگر PuTTY نمی‌خواهید:

```bash id="sshwin"
ssh root@192.168.1.1
```

---

## 🍎 Linux / macOS

در ترمینال:

```bash id="sshmac"
ssh root@192.168.1.1
```

---

## ⚠️ نکات مهم

* اگر وصل نشد:

  * مطمئن شوید کامپیوتر به روتر وصل است
  * آی‌پی ممکن است غیر از 192.168.1.1 باشد
* اولین اتصال ممکن است هشدار امنیتی بدهد (yes بزنید)

---

## 🎯 نتیجه این مرحله

اگر درست انجام شود باید وارد محیطی شوید که شبیه این است:

```
root@OpenWrt:~#
```

اوکی 👍 فهمیدم چی می‌خوای:
این بخش باید بگه **ورژن OpenWrt + معماری CPU** رو چطور دربیارن (نه RAM و چیزای اضافه)

این رو دقیق بذار داخل README 👇

---

## 📊 مرحله 1: پیدا کردن نسخه OpenWrt و معماری CPU

قبل از دانلود Passwall2 باید دقیقاً بدانید:

* نسخه OpenWrt
* معماری CPU دستگاه

---

## 🧠 1. بررسی نسخه OpenWrt

وارد SSH شوید و این دستور را بزنید:

```bash id="v1"
cat /etc/openwrt_release
```

---

### 📌 خروجی نمونه:

```id="v2"
DISTRIB_RELEASE='23.05.2'
DISTRIB_ARCH='aarch64_cortex-a53'
```

---

## 📌 معنی موارد مهم:

* `DISTRIB_RELEASE` → نسخه OpenWrt
* `DISTRIB_ARCH` → معماری CPU

---

## 🧠 2. روش سریع‌تر (فقط معماری)

```bash id="v3"
opkg print-architecture
```

یا:

```bash id="v4"
uname -m
```

---

### 📌 خروجی‌ها:

| خروجی       | معنی               |
| ----------- | ------------------ |
| x86_64      | کامپیوتر / مینی PC |
| aarch64     | روترهای 64 بیت     |
| armv7 / arm | روترهای معمولی     |
| mips        | دستگاه‌های قدیمی   |

---

## ⚠️ خیلی مهم

باید **هر دو مورد درست باشد**:

* نسخه OpenWrt (مثلاً 22 / 23 / 24)
* معماری CPU (arm / aarch64 / x86)

اگر اشتباه انتخاب کنید:

❌ پکیج نصب نمی‌شود
❌ یا ارور architecture می‌دهد
❌ یا Passwall اجرا نمی‌شود

---

## 🎯 نتیجه این مرحله

بعد از این باید دقیق بدانید:

✔ OpenWrt چند است
✔ CPU چه معماری دارد

---

## 🚀 مرحله بعدی

حالا می‌رویم سراغ:

👉 انتخاب فایل درست از SourceForge
👉 دانلود Passwall2 مناسب دستگاه
👉 نصب دستی

---
سؤال خیلی خوبیه 👌 و این دقیقاً چیزی هست که باید تو README هم شفاف بگی.

## 🧠 جواب کوتاه:

نه، در بیشتر موارد **روش پیدا کردن مشخصات CPU بین OpenWrt 23 / 24 / 25 فرق خاصی نداره**
ولی **خروجی و جزئیات نسخه‌ها کمی فرق می‌کنه**.

---

## 📊 توضیح دقیق و ساده:

### 🟢 چیزی که همیشه ثابت می‌مونه:

این دستورات تقریباً در همه نسخه‌ها یکی هستن:

```bash
cat /etc/openwrt_release
```

```bash
opkg print-architecture
```

```bash
uname -m
```

✔ این‌ها در 23 / 24 / 25 هم کار می‌کنن
✔ برای فهمیدن architecture همیشه قابل اعتمادن

---

## 🔴 چیزی که تغییر کرده (مهمه برای پروژه تو):

### 1. نسخه 23.x

* هنوز opkg اصلیه
* ساختار ساده‌تر
* خروجی‌ها کلاسیک‌تر

---

### 2. نسخه 24.x

* همچنان opkg
* ولی kernel و package ها جدیدتر شدن
* بعضی اسم پکیج‌ها دقیق‌تر شدن

---

### 3. نسخه 25.x (خیلی مهم)

طبق مستندات رسمی:

* ❗ package manager از **opkg → apk** تغییر کرده ([Wikipedia][1])
* بعضی دستورات opkg ممکنه نباشن یا محدود بشن
* ساختار سیستم جدیدتر شده

---

## ⚠️ نتیجه مهم برای آموزش تو:

تو باید تو README اینو بنویسی:

```markdown id="note1"
## ⚠️ توجه به نسخه OpenWrt

در نسخه‌های مختلف OpenWrt (23 / 24 / 25):

- روش پیدا کردن معماری تقریباً یکسان است
- اما در OpenWrt 25 ممکن است به جای opkg از apk استفاده شود
- بنابراین بعضی دستورها ممکن است متفاوت باشند
```

---

## 💡 پیشنهاد حرفه‌ای برای پروژه تو:

به جای اینکه فقط بگی “این دستور رو بزن”، این کارو کن:

### 🔥 حالت هوشمند (بهترین برای کاربرها)

```markdown id="smart1"
## 📌 تشخیص خودکار سیستم

اگر این دستور کار کرد:

opkg print-architecture

اگر کار نکرد (در OpenWrt 25):

apk info
```

---

## 🎯 جمع‌بندی ساده:

* ✔ روش پیدا کردن CPU/Architecture تقریباً یکیه
* ⚠️ فقط OpenWrt 25 تغییر بزرگ داره (apk به جای opkg)
* ✔ برای Passwall2 هنوز 23/24 رایج‌تر و پایدارترن

---

اگر بخوای، مرحله بعدی رو خیلی حرفه‌ای می‌کنم برات:
👉 “تشخیص خودکار نسخه OpenWrt + پیشنهاد لینک دانلود درست”
طوری که کاربر فقط یه دستور بزنه و خودش بفهمه چی دانلود کنه 👍

[1]: https://en.wikipedia.org/wiki/OpenWrt?utm_source=chatgpt.com "OpenWrt"


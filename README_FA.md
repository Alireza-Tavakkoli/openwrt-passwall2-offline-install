# آموزش نصب Passwall2 به صورت دستی روی OpenWrt
<br>

## مقدمه
در این آموزش یاد می‌گیرید چگونه Passwall2 را به صورت دستی (Manual) روی OpenWrt نصب کنید.

این روش برای زمانی است که:
- اینترنت آزاد بر روی روتر ندارید یا در کشوری زندگی میکنید که از همجا فیلتر است مثلا ایران
- یا می‌خواهید فایل‌ها را خودتان نصب کنید
- یا ابزار opkg / apk به درستی کار نمی‌کند
<br>

## ⚠️ حداقل مشخصات مورد نیاز

قبل از نصب، مطمئن شوید دستگاه شما توانایی اجرای Passwall2 را دارد:

### 🔹 رم (RAM)
- حداقل قابل اجرا: 128MB  
- پیشنهادی: 256MB یا بیشتر  
- کمتر از 128MB → احتمال لگ، کرش یا اجرا نشدن سرویس‌ها

### 🔹 پردازنده (CPU)
- حداقل پیشنهادی: 700MHz به بالا  
- بهتر: دو هسته‌ای یا 1GHz+

### 🔹 حافظه (Storage)
- حداقل: 16MB فضای خالی (لب مرز)  
- پیشنهادی: 32MB تا 128MB  
- چون Passwall2 حجم می‌گیرند

### 🔹 نسخه OpenWrt
- حداقل: OpenWrt 21.02  
- پیشنهادی: 23 / 24 / 25  
<br>

## 🔌 مرحله 1 : اتصال به روتر

قبل از هر کاری باید به روتر OpenWrt وصل شوید.

## 🟢 روش اول: PuTTY (ویندوز)

📥 دانلود:

👉 [https://soft98.ir/internet/network/3197-putty.html]


### 📌 مراحل اتصال

1. برنامه PuTTY را باز کنید
2. در قسمت **Host Name** این را وارد کنید:

```
192.168.1.1
```

3. روی **Open** کلیک کنید



### 🔑 ورود به روتر

وقتی صفحه سیاه باز شد:

* Username :

```
root
```

* Password :

```
رمز روتر (اگر تنظیم نکردی خالی بگذار)
```



## 🟡 روش دوم: Windows Terminal (جایگزین PuTTY)

اگر PuTTY نمی‌خواهید:

```bash id="sshwin"
ssh root@192.168.1.1
```

<br>

## 🍎 لینوکس / macOS

در ترمینال:

```bash id="sshmac"
ssh root@192.168.1.1
```



## ⚠️ نکات مهم

*اگر وصل نشد:
<br>
*مطمئن شوید کامپیوتر به روتر وصل است
*آی‌پی ممکن است غیر از 192.168.1.1 باشد
*اولین اتصال ممکن است هشدار امنیتی بدهد (yes بزنید)



## 🎯 نتیجه این مرحله

اگر درست انجام شود باید وارد محیطی شوید که شبیه این است:

```
root@OpenWrt:~#
```
<br>
## 📊 مرحله 2 : پیدا کردن نسخه OpenWrt و معماری CPU

قبل از دانلود Passwall2 باید دقیقاً بدانید:

* نسخه OpenWrt
* معماری CPU دستگاه

---

## 🧠 1. بررسی نسخه OpenWrt

وارد SSH شوید و این دستور را بزنید:

```bash id="v1"
cat /etc/openwrt_release
```

---

### 📌 خروجی نمونه:

```id="v2"
DISTRIB_RELEASE='23.05.2'
DISTRIB_ARCH='aarch64_cortex-a53'
```

---

## 📌 معنی موارد مهم:

* `DISTRIB_RELEASE` →  OpenWrt نسخه
* `DISTRIB_ARCH` →  CPU معماری 

---
اگر در یاداشت معماری پردازنده و نسخه OpenWrt اشتباه کنید:

❌ پکیج نصب نمی‌شود
❌ یا ارور architecture می‌دهد
❌ یا Passwall اجرا نمی‌شود


## 📥 مرحله 2: دانلود فایل‌های مناسب Passwall2

حالا که نسخه OpenWrt و معماری دستگاه خود را می‌دانید، باید فایل‌های مناسب دستگاه خود را دانلود کنید.

### مثال

اگر خروجی شما این باشد:

```text
DISTRIB_RELEASE='24.10'
DISTRIB_ARCH='aarch64_cortex-a53'
```

باید فایل‌های مربوط به:

* OpenWrt 24.10
* aarch64_cortex-a53

را دانلود کنید.

---

## 🌐 دانلود فایل‌ها

به صفحه زیر بروید:

```text
https://sourceforge.net/projects/openwrt-passwall-build/files/
```

---

## 📂 انتخاب نسخه صحیح

ابتدا نسخه OpenWrt خود را انتخاب کنید:

* OpenWrt 23
* OpenWrt 24
* OpenWrt 25

سپس معماری مناسب دستگاه خود را انتخاب کنید.

---

## 📦 فایل‌های مورد نیاز

معمولاً حداقل به این فایل‌ها نیاز دارید:

* luci-app-passwall2
* passwall2

در بعضی نسخه‌ها ممکن است وابستگی‌ها (Dependencies) نیز لازم باشند.

---

## ⚠️ قبل از دانلود

حتماً مطمئن شوید:

✅ نسخه OpenWrt فایل با نسخه روتر شما یکی باشد.

✅ معماری فایل با معماری دستگاه شما یکی باشد.

در غیر این صورت با خطاهایی مثل:

```text
incompatible with the architectures
```

یا

```text
cannot satisfy dependencies
```

مواجه خواهید شد.

---

### پیشنهاد

بعد از این مرحله، به جای رفتن مستقیم سراغ نصب، یک **مرحله 3: انتقال فایل‌ها به روتر** بذار.

ترتیب حرفه‌ای آموزش اینطوری میشه:

1. اتصال با SSH
2. پیدا کردن نسخه OpenWrt و معماری CPU
3. دانلود فایل‌های مناسب
4. انتقال فایل‌ها به روتر (WinSCP / SCP)
5. نصب Passwall2
6. فعال‌سازی و بررسی نصب

این ترتیب برای کسی که تازه‌کاره خیلی قابل فهم‌تره.
اوکی 👍 مرحله بعد طبیعی اینه: **انتقال فایل‌ها به روتر (قبل از نصب)**

این رو مستقیم بذار تو README:

---
اوکی 👍 فقط یه اصلاح مهم: تو این روش **اصلاً “کلید جداگانه” لازم نداریم** چون نصب دستی از SourceForge انجام میشه. اگر کاربر رو ببری سمت “کلید”، فقط گیج میشه.

اما چیزی که تو می‌خوای در واقع اینه:
👉 اول راهنمایی انتخاب فایل درست + بعد دانلود پکیج

این نسخه درست و تمیزش برای README 👇

---

## 📥 مرحله 2: دانلود فایل‌ها از SourceForge

وارد این صفحه شوید:

👉 [https://sourceforge.net/projects/openwrt-passwall-build](https://sourceforge.net/projects/openwrt-passwall-build)

---

## ⚠️ قبل از دانلود (خیلی مهم)

قبل از انتخاب فایل، باید نسخه OpenWrt خود را مشخص کرده باشید:

* OpenWrt 24 و پایین‌تر
* OpenWrt 25 و بالاتر

---

## 📌 انتخاب نوع فایل

### 🟡 اگر OpenWrt 24 یا پایین‌تر دارید:

👉 وارد پوشه‌های **IPK** شوید و فایل‌های `.ipk` را دانلود کنید

مثل:

* passwall2.ipk
* luci-app-passwall2.ipk

---

### 🔵 اگر OpenWrt 25 یا بالاتر دارید:

👉 وارد بخش **APK / snapshot packages** شوید و فایل‌های مربوطه را دانلود کنید

مثل:

* packages.apk
* یا بسته‌های snapshot

---

## 📌 قانون ساده

```text id="rule2"
OpenWrt ≤ 24  → IPK
OpenWrt ≥ 25  → APK
```

---

## 📂 بعد از دانلود

فایل‌ها را آماده کنید برای انتقال به روتر:

* همه فایل‌ها باید داخل `/tmp/` منتقل شوند

---

## ⚠️ نکته مهم

* فایل اشتباه = نصب خراب ❌
* IPK و APK قابل جایگزینی نیستند ❌
* حتماً معماری هم باید درست باشد (arm / x86 / mips)

---
اوکی 👍 اینو دقیق و تمیز می‌کنیم برای مرحله ۲ طوری که هم درست باشه هم کاربر گیج نشه.

فقط یه نکته مهم از SourceForge و نسخه‌ها:

* روی بعضی ریلیزها هنوز **IPK رایجه**
* روی نسخه‌های جدیدتر (25 به بالا / snapshot) ممکنه **APK یا بسته‌های apk-based feed** باشه ([SourceForge][1])

پس من متن رو برات **استاندارد و درست برای README** می‌نویسم 👇

---

## 📥 مرحله 2: دانلود فایل‌ها از SourceForge

وارد این صفحه شوید:

👉 [https://sourceforge.net/projects/openwrt-passwall-build](https://sourceforge.net/projects/openwrt-passwall-build)

---

## ⚠️ انتخاب فایل درست (خیلی مهم)

در این صفحه بسته به نسخه OpenWrt شما باید فایل مناسب را انتخاب کنید:

---

### 🟡 اگر OpenWrt شما 24 یا پایین‌تر است:

👉 باید فایل‌های **IPK** را دانلود کنید

📦 مثال:

* `luci-app-passwall2_*.ipk`
* `passwall2_*.ipk`
* dependency ها با فرمت `.ipk`

---

### 🔵 اگر OpenWrt شما 25 یا بالاتر است:

👉 باید فایل‌های **APK / snapshot packages** را دانلود کنید

📦 مثال:

* `passwall_packages_apk_*.zip`
* یا فایل‌های `.apk`
* یا feeds مخصوص apk

---

## 📌 قانون ساده (خیلی مهم)

```text id="rule1"
OpenWrt ≤ 24  → IPK
OpenWrt ≥ 25  → APK
```

---

## 📂 بعد از دانلود

فایل‌ها را استخراج کنید و آماده انتقال به روتر کنید:

* `.ipk` یا `.apk` باید داخل `/tmp/` منتقل شوند

---

## ⚠️ نکته مهم

* IPK و APK با هم قابل نصب نیستند ❌
* اگر اشتباه انتخاب کنید:

  * dependency error
  * architecture error
  * یا نصب کامل انجام نمی‌شود

---

## 🎯 نتیجه این مرحله

بعد از این مرحله شما دارید:

✔ فایل درست مخصوص نسخه OpenWrt
✔ آماده برای انتقال به روتر
✔ بدون خطای معماری یا پکیج

---

## 🚀 مرحله بعد

👉 انتقال فایل‌ها به روتر (WinSCP / SCP)
👉 سپس نصب دستی Passwall2

---

اگر بخوای، مرحله بعدی رو خیلی حرفه‌ای می‌کنم که:
👉 کاربر فقط بگه “نسخه‌م چیه” و خودت بهش بگی چی دانلود کنه 😄

[1]: https://sourceforge.net/projects/openwrt-passwall.mirror/files/25.12.25-1/?utm_source=chatgpt.com "OpenWrt PassWall - Browse /25.12.25-1 at SourceForge.net"

## 🎯 نتیجه این مرحله

✔ فایل درست دانلود شده
✔ آماده انتقال به روتر
✔ بدون خطای نصب

---

اگر بخوای مرحله بعدی رو می‌ریم 👍
👉 انتقال فایل‌ها + نصب واقعی Passwall2 (اونجایی که کار تموم میشه)



## 📤 مرحله 3: انتقال فایل‌ها به روتر

بعد از دانلود فایل‌های Passwall2 باید آن‌ها را به روتر OpenWrt منتقل کنید.

---

## 🟢 روش 1: WinSCP (ویندوز - ساده‌ترین روش)

### 📥 دانلود:

👉 [https://winscp.net/](https://winscp.net/)

---

### 📌 مراحل:

1. برنامه WinSCP را باز کنید
2. این اطلاعات را وارد کنید:

* File protocol: `SCP` یا `SFTP`
* Host name: `192.168.1.1`
* Username: `root`
* Password: رمز روتر

3. روی Login بزنید

---

### 📂 انتقال فایل

فایل‌های `.ipk` را بکشید داخل این مسیر:

```
/tmp/
```

---

## 🟡 روش 2: PuTTY + SCP (دستی)

اگر ترمینال دارید:

```bash id="scp1"
scp passwall2.ipk root@192.168.1.1:/tmp/
```

---

## 🍎 Linux / macOS

```bash id="scp2"
scp passwall2.ipk root@192.168.1.1:/tmp/
```

---

## ⚠️ نکات مهم

* فایل‌ها باید داخل `/tmp/` باشند
* اگر چند فایل دارید همه را منتقل کنید
* ترتیب مهم است (اول dependency ها بعد خود Passwall2)

---

## 🎯 نتیجه این مرحله

اگر درست انجام شود:

✔ فایل‌ها داخل روتر هستند
✔ آماده نصب با opkg هستید

---

## 🚀 مرحله بعد

حالا می‌رویم سراغ:

👉 نصب دستی Passwall2 روی OpenWrt
👉 رفع خطاهای dependency
👉 فعال‌سازی سرویس

---

اگر بخوای، مرحله نصب رو هم برات خیلی تمیز و بدون خطا می‌نویسم 👍
اوکی 👍 بریم مرحله ۳ رو تمیز و بازنویسی کنیم: **انتقال فایل‌ها به روتر**

این نسخه رو مستقیم بذار تو README 👇

---

## 📤 مرحله 3: انتقال فایل‌ها به روتر (OpenWrt)

بعد از دانلود فایل‌های Passwall2، باید آن‌ها را به روتر منتقل کنید تا بتوانید نصب را انجام دهید.

---

## 🟢 روش 1: WinSCP (پیشنهادی برای ویندوز)

### 📥 دانلود:

👉 [https://winscp.net/](https://winscp.net/)

---

### 📌 مراحل اتصال:

1. برنامه WinSCP را باز کنید
2. اطلاعات زیر را وارد کنید:

* File protocol: `SCP` یا `SFTP`
* Host name: `192.168.1.1`
* Port: `22`
* Username: `root`
* Password: رمز روتر

3. روی Login کلیک کنید

---

### 📂 انتقال فایل‌ها:

فایل‌های دانلود شده را به این مسیر منتقل کنید:

```bash id="tmp3"
/tmp/
```

---

## 🟡 روش 2: SCP (لینوکس / macOS / Windows Terminal)

اگر ترمینال دارید:

```bash id="scp3"
scp passwall2.ipk root@192.168.1.1:/tmp/
```

اگر چند فایل دارید:

```bash id="scp4"
scp *.ipk root@192.168.1.1:/tmp/
```

---

## 🍎 روش 3: Linux / macOS ساده

```bash id="scp5"
scp file.ipk root@192.168.1.1:/tmp/
```

---

## ⚠️ نکات مهم

* همه فایل‌ها باید داخل `/tmp/` باشند
* اگر چند فایل دارید، همه را منتقل کنید
* ترتیب مهم است (اول dependency ها بعد main package)

---

## 🎯 نتیجه این مرحله

اگر درست انجام شود:

✔ فایل‌ها داخل روتر هستند
✔ آماده نصب با opkg هستید

---

## 🚀 مرحله بعد

👉 نصب دستی Passwall2 روی OpenWrt
👉 رفع خطاهای dependency
👉 فعال‌سازی سرویس

---

اگر بخوای، مرحله ۴ (نصب اصلی) رو هم برات خیلی تمیز و بدون دردسر می‌نویسم 👍
`
